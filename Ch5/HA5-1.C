#include<stdio.h>
#include<conio.h>
    void	main()
	{      int first,second;
		clrscr();
	printf("Enter the first value:");
	scanf("%d",&first);
	printf("Enter the second value:");
	scanf("%d",&second);
	if (first>second)
	{
	printf("minimum value is %d",second);
	}
	else
	{
	printf("minimum value is %d",first);

	}
	getch();
	}













		getch()
			}
